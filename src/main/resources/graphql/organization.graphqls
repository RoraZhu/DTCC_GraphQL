type Organization {
    orgId: ID!
    orgName: String
    orgLongName: String
    familyId: ID
    startDate: String
    endDate: String
    orgType: String
    esgRegionCode: String
    privateFlag: String
    description: String
    bicLidPriorityId: ID
    marketId: ID
}

extend type Query {
    # findAllOrganizations: [Organization]!
    findOrganizationById(id: ID): Organization
}